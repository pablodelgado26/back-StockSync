# ========================================
# 🔐 VARIÁVEIS DE AMBIENTE - StockSync
# ========================================
# 
# Este é um arquivo de exemplo. Para usar:
# 1. Copie este arquivo: cp .env.example .env (ou renomeie no Windows)
# 2. Edite o .env com suas configurações
# 3. NUNCA commite o arquivo .env (já está no .gitignore)

# ====== BANCO DE DADOS ======
# URL de conexão do banco de dados
# SQLite (desenvolvimento): file:./dev.db
# PostgreSQL (produção): postgresql://user:password@localhost:5432/dbname
DATABASE_URL="file:./dev.db"

# ====== AUTENTICAÇÃO JWT ======
# Chave secreta para assinar tokens JWT
# ⚠️ IMPORTANTE: Em produção, use uma chave forte e aleatória!
# Como gerar: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET="sua-chave-secreta-super-segura-aqui"

# Tempo de expiração do token
# Exemplos: 1h (1 hora), 24h (1 dia), 7d (7 dias), 30d (30 dias)
JWT_EXPIRES_IN="7d"

# ====== SERVIDOR ======
# Porta do servidor Express
PORT=4000

# Ambiente de execução
# Valores: development | production | test
NODE_ENV="development"

# ====== CORS (opcional) ======
# URL do frontend (descomentar se necessário)
# FRONTEND_URL="http://localhost:3000"

# ====== OBSERVAÇÕES ======
# - Em desenvolvimento: As configurações acima são suficientes
# - Em produção: 
#   * Use PostgreSQL ou MySQL em vez de SQLite
#   * Gere uma JWT_SECRET forte e aleatória
#   * Configure NODE_ENV="production"
#   * Configure CORS adequadamente
